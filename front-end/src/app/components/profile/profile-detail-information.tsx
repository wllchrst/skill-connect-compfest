import { IUser } from "@/app/interfaces/user-interface";

interface I {
  user: IUser;
}
function ProfileDetailInformation({ user }: I) {
  return <div></div>;
}

export default ProfileDetailInformation;
